window:
  dimensions:
    columns: 192
    lines: 96
shell:
  # fish_add_path <cargo_path> if not set
  program: /usr/local/bin/fish
  args:
    - --login
key_bindings:
  # Pass Cmd + p into nvim
  - { key: P, mods: Command, chars: "\x1bp"                 }
